# U-0.1 — Fail-closed validation in the add path
Concept: INV-5. Pins baseline: yes. Invariants touched: INV-5, INV-10, INV-11.

## Purpose
Three catch blocks let a THROWN validation exception continue the add; the API advisory check has
a fourth. A malformed spec file currently disables the compatibility gate (audit A-1). Make every
validation exception abort the operation.

## Inputs (Files To Read — exact ranges, nothing else)
- core/models/server/ServerBuilder.php lines 440–470 (addComponent head, tx pattern)
- core/models/server/ServerBuilder.php lines 655–715 (Phase 5.1 CPU catch, Phase 5.5 RAM catch, Phase 6 catch)
- api/handlers/server/server_api.php lines 386–450 (advisory validation + its catch)
- tests/characterize_compatibility.php lines 1–60 (how to run/read the suite)

## Outputs
Blocking behavior on validation exceptions; regression test proving it.

## Files Modified (≤5)
1. core/models/server/ServerBuilder.php — in the three catch blocks, replace log-and-continue with:
   rollback if $ownTransaction && inTransaction(), then
   `return ['success'=>false,'message'=>'Validation failed (system error): '.$e->getMessage(),'error_type'=>'validation_exception'];`
   Keep the error_log lines. Delete the comments "Continue without …".
2. api/handlers/server/server_api.php — in handleAddComponent's catch (Exception $validationError):
   replace the warning-and-continue branch with
   `send_json_response(0, 1, 500, "Validation unavailable - request rejected (fail-closed)");`
   Delete the "$validationInfo = [...unavailable...]" line and its later response merge.

## Files Created
3. tests/regression/fail_closed_test.php — builds scratch config; monkey-path: temporarily rename a
   required spec JSON in a tmp ims-data copy (ComponentDataService path override) so
   validateComponentAddition throws; asserts add returns success=false AND no row/JSON mutation
   occurred AND inventory Status unchanged. Restore file in finally.

## Database Changes
None.

## Tests Required (Acceptance Tests)
```
php -l core/models/server/ServerBuilder.php && php -l api/handlers/server/server_api.php
php tests/regression/fail_closed_test.php                     # PASS
php tests/characterize_compatibility.php                      # diffs ONLY of class: previously-successful add now blocked with error_type=validation_exception
grep -rn "Continue without" core/ api/                        # empty (INV-5 check)
```
Baseline change allowed: entries whose fixtures relied on swallowed exceptions.
Commit note: `BASELINE-CHANGE: audit A-1 fail-open removed`.

## Rollback Procedure
`git revert` the unit commit(s). No schema, no flags.

## Completion Criteria
All four commands above pass; ≤3 files touched; ≤120 LOC delta.

## Human Verification Checklist
- [ ] The three ServerBuilder catches now return failure and roll back own transactions
- [ ] API catch responds 500, never proceeds
- [ ] No other logic changed in the touched ranges
- [ ] Baseline diff reviewed: every changed entry is an A-1 case
