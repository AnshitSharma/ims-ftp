# U-0.4 — Verification harness + baselines
Concept: observability bootstrap. Pins baseline: creates it. Invariants touched: INV-11.

## Purpose
Create scripts/verify/run_all.php plus the three reports that need no new schema
(inventory_report, orphan_report, performance_report), capture perf + characterization baselines.
Every later gate calls this harness.

## Inputs (Files To Read)
- migration/11-verification/README.md (the report contracts — implement exactly)
- scripts/audit-orphans.php 1–80 (bootstrap + PDO pattern to copy)
- tests/fixture_scenarios_real.php 1–50 (scenario list for perf replay)
- tests/golden/setup_scratch_db.sql 1–30 (scratch DB creation)

## Outputs
scripts/verify/{run_all.php, inventory_report.php, orphan_report.php, performance_report.php},
reports/perf-baseline.json, reports/.gitignore (`*`, `!.gitignore`, `!perf-baseline.json`).

## Files Created (5) / Modified (0)
As listed above. run_all.php: implements the contract in 11-verification/README.md §run_all;
report registry is a const array mapping report name → script path → gate list (copy gate lists
from phase-status.json). Reports not yet implemented print `<name>: SKIPPED (lands in <unit>)`
and do not affect exit code — the registry marks them `"available": false` until their unit flips it.

## Database Changes
None.

## Tests Required (Acceptance Tests)
```
php scripts/verify/run_all.php --quick        # exit 0; inventory+orphan GREEN, others SKIPPED
php scripts/verify/run_all.php --gate P0      # exit 0
test -s reports/perf-baseline.json
php scripts/verify/inventory_report.php --self-test   # must exit 1 (detects seeded defect fixture)
```

## Rollback Procedure
Delete created files; git revert.

## Completion Criteria
Harness runs both modes; self-tests prove detection; baselines committed.

## Human Verification Checklist
- [ ] SKIPPED reports cannot green-wash a gate (registry flag checked)
- [ ] perf baseline captured AFTER U-0.1..0.3 (guards included in baseline)
