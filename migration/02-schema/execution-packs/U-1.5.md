# U-1.5 — Dual-write hooks (flag OFF by default)
Concept: dual-write window opens (INV-8 substrate). Pins baseline: yes (must show ZERO diffs).
Invariants touched: INV-5, INV-8, INV-11, INV-12.

## Purpose
When DUAL_WRITE_ENABLED=on, every legacy JSON mutation also writes rows via the repository, in the
SAME transaction, fail-closed. Default off ⇒ byte-identical production behavior.

## Inputs (Files To Read)
- core/models/server/ServerBuilder.php 2293–2360 (updateServerConfigurationTable dispatch — hook point)
- core/models/server/ServerBuilder.php 800–870 (add write phase: where serial/slot/options are in scope)
- core/models/server/ServerBuilder.php 1030–1060 (remove write phase)
- core/models/config/ConfigComponentRepository.php (your API)
- 00-overview/FLAGS.md (flag pattern)

## Files Created (1) / Modified (1)
CREATE core/models/config/ConfigComponentWriter.php — static flag reader `mode()` (off|on) +
`afterLegacyAdd(PDO,$configUuid,$type,$specUuid,$serial,$slotRef,$inventoryTable,$inventoryId,$actor)`
and `afterLegacyRemove(...)`; each: if mode off → return; else call repository (insert/tombstone).
parent_id resolution here is BEST-EFFORT NULL (backfill U-B.2 owns real parents; writer sets parent
only for the trivially known cases: sfp→parent_nic_uuid, onboard nic→motherboard row).
MODIFY core/models/server/ServerBuilder.php — exactly two call sites: at the end of the add write
phase (after updateServerConfigurationTable(...,'add',...) succeeds, same tx) and after the remove
write. Inventory id/table: reuse `$componentDetails['ID']` + getComponentInventoryTable(); for
remove, resolve via repository findLive(). Any writer exception propagates (fail-closed; the
surrounding ownTransaction handling from U-0.1/0.3 rolls everything back).

## Database Changes
None.

## Tests (Acceptance)
```
php tests/characterize_compatibility.php                          # flag off: ZERO diffs
DUAL_WRITE_ENABLED=on php tests/regression/dual_write_test.php    # NEW test: add→row appears same tx; remove→tombstone; induced repo failure rolls back JSON too
DUAL_WRITE_ENABLED=on php tests/characterize_compatibility.php    # verdicts still ZERO diffs (rows are invisible to verdicts)
grep -rn "DUAL_WRITE_ENABLED" core/ | grep -v ConfigComponentWriter  # empty
```
Files Created also includes tests/regression/dual_write_test.php (counts toward the 5-file box: 2 created + 1 modified = 3).

## Rollback
Flag already off; git revert.

## Checklist
- [ ] Writer called INSIDE the existing transaction, after legacy write, before commit
- [ ] Failure path proven to roll back BOTH stores
- [ ] parent_id best-effort rule respected (no spec loading in the writer)
