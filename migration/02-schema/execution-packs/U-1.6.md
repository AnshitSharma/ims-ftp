# U-1.6 — equivalence_report.php (INV-8 owner)
Concept: JSON↔rows equivalence proof. Pins baseline: no. Invariants touched: INV-8, INV-11.

## Inputs (Files To Read)
- migration/11-verification/README.md §equivalence_report (the contract — implement exactly)
- core/models/server/ServerBuilder.php 61–120 (extractComponentsFromJson signature/output shape ONLY)
- core/models/config/ConfigComponentRepository.php (liveRows shape)

## Files Created (1) / Modified (1)
scripts/verify/equivalence_report.php per contract: canonical tuple
[component_type, spec_uuid, serial_number|null, slot_ref|null] sorted; modes --all (skip is_virtual=1 rows — see F-5; keyset paging,
1000/batch, resumable via --after <uuid>) and --config <uuid>; output JSON diff list; exit 0 iff empty.
Normalization rules (write as a const, they encode legacy quirks):
  legacy 'pciecard' with subtype Riser Card ⇒ compare as 'riser';
  onboard NICs (uuid prefix 'onboard-') excluded from BOTH sides until U-B.2 lands (flagged const TODO_UB2=false→true in U-B.2);
  hbacard legacy scalar hbacard_uuid counts as one live entry when hbacard_config empty.
MODIFY run_all.php: register, available:true.

## Tests
```
php scripts/verify/equivalence_report.php --config <scratch-fixture>   # exit 0 on dual-written fixture
php scripts/verify/equivalence_report.php --self-test                  # exit 1 (fixture with an injected diff)
php scripts/verify/run_all.php --quick                                 # includes equivalence, GREEN on scratch
```

## Rollback
Delete file; unregister.

## Checklist
- [ ] Canonicalization consts documented with the legacy quirk each encodes
- [ ] Self-test proves detection  - [ ] Keyset pagination (no OFFSET)
