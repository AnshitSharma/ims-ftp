# Handoff — INDEPENDENT VERIFY: U-L.3 + PL GATE OPEN — 2026-07-07

## Current State
U-L.3 verified (`implemented` → `verified`); **PL gate is now OPEN** — all three PL units verified
and all gate reports pass. U-B.1 (07-component-migration) is unblocked. Verified against commit
`b55e01f` on `claude/migration-directory-impl-yk39zk`.

## Gate-report accounting (rule: all gate_reports exited 0)
- `run_all.php --gate PL` → exit 0: schema GREEN, **ledger GREEN**, regression SKIPPED-by-design.
- `regression` leg covered by hand: all 5 `tests/regression/*.php` + both `tests/unit/*.php`
  green, exit 0 each.
- Additionally `--quick` → exit 0 (schema/inventory/orphan/equivalence all GREEN).

## What was re-run (scratch MariaDB + stub ims-data, this container)
- `slot_report.php` → GREEN, exit 0; `--self-test` → exit 1 PASS (detection proven).
- `ledger_report.php` → GREEN, exit 0; `--self-test` → exit 1 PASS.
- `php -l` clean on both new scripts.
- Zero fixture residue: `config_components` and `config_resources` both empty after all suites.
- Golden baseline untouched (working tree clean).

## Claims spot-verified in code
- RV-4 is REAL: `PcieLaneBudgetValidator` excludes M.2 NVMe from the shared expansion-lane budget
  (TP-1C comments at lines ~211 and ~325), but `ResourceCatalog::consumesStorage()` (lines
  125-135) passes every `extractStoragePCIeLanes()` value straight into `pcie_lane` consumption
  with no M.2 exclusion. Latent today (cpu `provides()` gap blocks the whole lane path) but MUST
  be fixed together with the CPU catalog gap, or ledger_report check 4 will RED on every M.2 build
  the moment lanes become live. RV-4 joins RV-1/RV-2/RV-3.
- slot_report's interpretation calls (live-provider check via tombstone-bypass self-test; onboard
  NIC exclusion) match the code and are consistent with equivalence_report's precedent.

## Outstanding items (cumulative — unchanged plus RV-4)
1. ResourceCatalog field-name gaps (cpu/nic provides; nic/hbacard/pciecard consumes; motherboard
   sockets/DIMMs; chassis bays) — needs real `ims-data/` field names from the human.
2. RV-1 (DIMM linking), RV-2 (slot_ref scheme reconciliation), RV-3 (provider-removal ledger
   drift), RV-4 (M.2 lane exclusion) — bundle with the catalog-gap fix unit.
3. RAM serial_number legacy extraction fix before DUAL_WRITE_ENABLED=on.
4. Seeders 2026_07_06_001..003 still not applied to the production DB.
5. `RISER_SUBTYPE_KEYS` narrowing in equivalence_report.php.

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-5 | PASS — both reports fail-closed (RED/exception) rather than skipping silently |
| INV-11 (this session) | PASS — verification-only: phase-status.json + this handoff |

## Next Prompt To Use
"Continue the IMS migration. Read migration/00-overview/README.md,
migration/ARCHITECTURAL_INVARIANTS.md, then execute unit U-B.1 using
migration/07-component-migration/execution-packs/U-B.1.md. PL's gate is open (see
migration/handoffs/PL-GATE-20260707.md — note outstanding items, especially the ResourceCatalog
gaps if U-B.1's backfill touches ledger writes). ONE unit only; leave 'verified' to a separate
session. Follow migration/00-overview/SESSION_PROTOCOL.md exactly."

## Files To Load Into Context (next session)
- migration/00-overview/README.md
- migration/ARCHITECTURAL_INVARIANTS.md
- migration/handoffs/PL-GATE-20260707.md
- migration/07-component-migration/execution-packs/U-B.1.md

## Expected Context Size
~25k tokens
