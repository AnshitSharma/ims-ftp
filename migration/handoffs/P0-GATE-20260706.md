# Handoff — P0 GATE VERIFICATION — 2026-07-06

## Current State
All four P0 units (U-0.1..U-0.4) re-verified in a fresh remote session and flipped
`implemented` → `verified`; the P0 gate is now **open** in `migration/phase-status.json`.
This was a verification-only session — zero product/test/script files changed. The only
edits are `phase-status.json` and this handoff.

## Completed Work (what was verified, and how)
Environment: fresh cloud container, no MySQL, no `ims-data/`, no production dump (same gap
as the U-0.3/U-0.4 sessions). Rebuilt the same scratch setup those sessions used: MariaDB
10.11 installed via apt, run manually against a scratchpad datadir; hand-built minimal
schema (`server_configurations` incl. `hbacard_uuid`, `server_configuration_history`,
`users`, all 10 `{type}inventory` tables); stub `../ims-data/ram/ram_detail.json` (contents
irrelevant — `fail_closed_test` copies then hides it to inject its fault).

Static/code verification (each unit's fingerprint confirmed in the tree at 93b5aa1):
- U-0.1: 4 fail-closed catches in `ServerBuilder.php` (lines 679/701/726/845 return
  `error_type=validation_exception`); `server_api.php:445` responds 500 fail-closed;
  `grep -rn "Continue without" core/ api/` → empty (INV-5).
- U-0.2: exactly 2 `TEMP-GUARD(U-0.2)` markers in `ServerBuilder.php`.
- U-0.3: all 4 `beginTransaction` sites guarded by `$ownTransaction`; every rollback/commit
  site conditional on ownership.
- U-0.4: all 4 `scripts/verify/` scripts present, `php -l` clean; `reports/.gitignore` +
  `perf-baseline.json` committed.

Dynamic verification (all against the scratch DB):
- `php tests/regression/fail_closed_test.php` → ALL PASS (7/7, exit 0)
- `php tests/regression/finalized_immutability_test.php` → ALL PASS (10/10, exit 0)
- `php tests/regression/nested_transaction_test.php` → ALL PASS (13/13, exit 0)
- `php scripts/verify/run_all.php --gate P0` → exit 0
  (`baseline: SKIPPED`, `orphan: GREEN`, `regression: SKIPPED` — see gate-report accounting below)
- `php scripts/verify/run_all.php --quick` → exit 0 (`inventory: GREEN`, `orphan: GREEN`,
  schema/equivalence SKIPPED as designed)
- `php scripts/verify/inventory_report.php --self-test` → exit 1 "PASS (defect fixture
  correctly flagged)" (1 = detection proven, per the pack)
- `php tests/characterize_compatibility.php` → runs, exit 0 — but 0 configs / 0 replays
  (no production dump here). The regenerated empty baseline was NOT kept:
  `tests/golden/compatibility_baseline.json` restored via `git checkout` immediately
  (working tree confirmed clean afterwards).

## Gate-report accounting (rule: all gate_reports exited 0)
- `orphan` → GREEN via `run_all.php --gate P0`, exit 0.
- `regression` → no dedicated script by design (U-0.4 handoff); satisfied by hand: all three
  `tests/regression/*.php` green, exit 0 each.
- `baseline` → no dedicated script by design; satisfied by hand with a caveat: the harness
  exits 0 here but replays 0 configs (no dump in this environment). Real-data parity was
  proven where it could be: U-0.1 and U-0.2 each ran the harness against the production-
  mirror dump on the dev machine with **0 diffs** (their handoffs), and U-0.3/U-0.4 are
  structurally outside the harness replay path (it calls only validateConfiguration/
  validateConfigurationEnhanced/validateComponentAddition — never removeComponent/
  deleteConfiguration/verify scripts). On that combined evidence the gate is opened.

## Remaining Work
None for P0. Next unit: U-1.1 (`migration/02-schema/execution-packs/U-1.1.md`).

## Known Risks (carried forward, unchanged)
- `reports/perf-baseline.json` is still NOT a real production-catalog baseline (U-0.4
  handoff). Re-capture on a machine with real `ims-data/` + dump before any gate that lists
  `performance` (P6/P8) is trusted.
- On the developer machine, a one-command re-check of the baseline leg is recommended when
  convenient: reseed `ims_compat_golden` from the dump, run
  `php tests/characterize_compatibility.php`, diff → expect 0 (U-0.1/U-0.2 already showed 0).
- P0 guards remain temporary (`TEMP-GUARD(U-0.2)` → replaced by U-SM.4, deleted by U-D.4).
- The characterization harness regenerates `tests/golden/compatibility_baseline.json` in
  place on every run — running it against an empty/partial DB silently clobbers the golden
  file. Restore from git if that happens (it happened, and was restored, this session).

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-5 (fail-closed) | PASS — grep empty; fail_closed_test green |
| INV-4 (finalized immutable) | PASS — finalized_immutability_test green |
| INV-10 (pin before change) | PASS — nothing changed; golden baseline byte-identical to committed version |
| INV-11 (unit box) | PASS — verification-only: 2 files touched (phase-status.json, this handoff), 0 code, 0 seeders |

## Next Prompt To Use
"Continue the IMS migration. Read migration/00-overview/README.md,
migration/ARCHITECTURAL_INVARIANTS.md, then execute unit U-1.1 using
migration/02-schema/execution-packs/U-1.1.md. P0's gate is open (see
migration/handoffs/P0-GATE-20260706.md). Follow
migration/00-overview/SESSION_PROTOCOL.md exactly."

## Files To Load Into Context (next session)
- migration/00-overview/README.md
- migration/ARCHITECTURAL_INVARIANTS.md
- migration/phase-status.json
- migration/02-schema/execution-packs/U-1.1.md

## Expected Context Size
~20k tokens
