# Handoff — P1 PARTIAL VERIFICATION + U-1.5 UNBLOCK ANSWERS — 2026-07-06

## Current State
U-1.1..U-1.4 re-verified in a fresh session and flipped `implemented` → `verified`.
**P1's gate stays CLOSED** — U-1.5 is `blocked` and U-1.6 `not_started`; the gate rule requires
all units verified. This session also **answers both questions U-1.5 blocked on** (see below),
so the next session can resume U-1.5 immediately.

## ANSWER 1 (U-1.1/U-1.4 open question): row-reuse interpretation is CONFIRMED
The human (via this verify session) confirms the U-1.1 decision: `uq_inventory_once` on
`(inventory_table, inventory_id)` — one row per physical unit EVER, reused via UPDATE
(tombstone = `removed_at` set; re-add = same row reactivated, `config_uuid`/`slot_ref` rewritten).
U-1.4's `INSERT ... ON DUPLICATE KEY UPDATE` implementation is the intended pattern. The pack's
NOTE 3 (NULL-distinct uniqueness "holds" for live rows) was factually wrong and the deviation was
correct. No revision to U-1.1/U-1.4 needed.

## ANSWER 2 (U-1.5 blocker): YES, every {type}inventory table HAS a numeric auto-increment `ID` PK
Proven against the REAL production system this session, read-only:
- `BaseFunctions::addComponent()` already returns `'id' => $pdo->lastInsertId()` on inventory
  inserts (core/helpers/BaseFunctions.php:973) — nonzero lastInsertId requires AUTO_INCREMENT.
- Production API list calls (`ram-list`, `cpu-list`, `storage-list`, `motherboard-list`,
  `nic-list`, `chassis-list`) all return an `ID` field per row (e.g. raminventory row ID=93,
  cpuinventory ID=101, storageinventory ID=57, motherboardinventory ID=55, nicinventory ID=240,
  chassisinventory ID=41).
So U-1.5 is **case 1** from its handoff: extend `lockAndCheckComponent()`'s SELECT list with `ID`
(one additive line — nothing currently reads it, so nothing can break), and populate
`config_components.inventory_id` from `$componentDetails['ID']`. The human confirms this third
`ServerBuilder.php` touch is in-scope for U-1.5 (the pack's "exactly two call sites" meant the two
NEW hook points, not a prohibition on the supporting SELECT change). `config_components.inventory_id
BIGINT UNSIGNED` needs no schema change.

## Verification performed (U-1.1..U-1.4)
Environment: same scratch MariaDB as P0-gate session (restarted; datadir persisted), production
collation applied to `server_configurations.config_uuid` (`utf8mb4_unicode_ci`) before seeding.
- All 3 seeders apply cleanly in order; `SHOW CREATE TABLE config_components` confirms
  `uq_inventory_once (inventory_table, inventory_id)`, `uq_slot_occupancy (config_uuid, slot_ref,
  removed_at)`, real FKs `fk_cc_config`/`fk_cc_parent`.
- `schema_report.php` → GREEN, exit 0; `--self-test` → exit 1 "PASS (induced defect correctly flagged)".
- `tests/unit/config_component_repository_test.php` → 20/20 PASS, including the
  tombstone→reactivate-same-row-id proof of the confirmed interpretation.
- All 3 `tests/regression/*.php` → PASS (P0 behavior intact).
- `run_all.php --gate P1` → exit 0 (schema GREEN, regression SKIPPED-by-design and covered by hand);
  `--quick` → exit 0 (schema/inventory/orphan GREEN).
- INV-9 round-trip: rollbacks applied in reverse order (003→002→001) then re-applied (001→002→003),
  `schema_report.php` GREEN again afterwards.

## Remaining Work (P1)
- U-1.5: resume with the two answers above (create `ConfigComponentWriter.php`, two hook sites +
  the one-line `ID` SELECT addition in `ServerBuilder.php`, `tests/regression/dual_write_test.php`
  — the dual-write test MUST cover remove→re-add staying one row, per the confirmed interpretation).
- U-1.6: equivalence_report.php.
- Gate: open only after U-1.5 + U-1.6 are verified and gate reports pass.

## Known Risks (carried forward)
- Seeders 2026_07_06_001..003 are applied only to scratch DBs so far — they must be run on the
  production DB manually before dual-write is ever flagged on (seeders are not auto-deployed).
- Same environment gap as all prior sessions: no production dump / `ims-data/` here; the
  characterization-baseline leg of any future gate still deserves one real-data run on the dev machine.

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-1 | PASS — key structure verified in DDL; reactivation proof in repo unit test |
| INV-6 | PASS — uq_config_rev present; revision/event lockstep proven by U-1.4's test |
| INV-9 | PASS — all 3 rollback pairs round-tripped on scratch DB |
| INV-11 | PASS (this session) — verification-only: phase-status.json + this handoff |

## Next Prompt To Use
"Continue the IMS migration. Read migration/00-overview/README.md,
migration/ARCHITECTURAL_INVARIANTS.md, then resume unit U-1.5 using
migration/02-schema/execution-packs/U-1.5.md. Both blocking questions are ANSWERED in
migration/handoffs/P1-VERIFY-20260706.md (inventory tables have a numeric ID PK — extend
lockAndCheckComponent()'s SELECT with ID; and the U-1.1 row-reuse-via-UPDATE interpretation is
human-confirmed). Follow migration/00-overview/SESSION_PROTOCOL.md exactly."

## Files To Load Into Context (next session)
- migration/00-overview/README.md
- migration/ARCHITECTURAL_INVARIANTS.md
- migration/handoffs/P1-VERIFY-20260706.md (the two answers)
- migration/handoffs/U-1.5-20260706.md (the original block)
- migration/02-schema/execution-packs/U-1.5.md

## Expected Context Size
~25k tokens
