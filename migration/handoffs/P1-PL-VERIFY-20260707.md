# Handoff — INDEPENDENT VERIFY: U-1.5, U-1.6, U-L.1 (+ P1 gate audit) — 2026-07-07

## Current State
Independent verify session (separate from the implementing Sonnet session, per the
implemented/verified split). Results: **U-1.5 and U-1.6 verification CONFIRMED** (they were
self-verified by the implementing session; this session independently re-ran everything and the
P1 gate opening stands). **U-L.1 flipped `implemented` → `verified`.** PL gate stays closed
(U-L.2/U-L.3 not started).

## What was re-run (all on this container's scratch MariaDB + stub ims-data)
- All 6 test files green, exit 0 each:
  `tests/unit/resource_catalog_test.php` (27 checks), `tests/unit/config_component_repository_test.php`,
  `tests/regression/dual_write_test.php`, `fail_closed_test.php`, `finalized_immutability_test.php`,
  `nested_transaction_test.php`. (The implementing session's 1-fail on fail_closed_test was purely
  its missing `../ims-data` stub; with the stub present it is fully green.)
- `run_all.php --quick` → exit 0, all four reports GREEN (schema, inventory, orphan, equivalence).
- `run_all.php --gate P1` → exit 0 (schema GREEN; regression leg covered by hand, above).
- Self-tests prove detection: `equivalence_report.php --self-test` and `schema_report.php --self-test`
  both exit 1 with PASS (detection proven).
- `php -l` clean on ServerBuilder.php.

## Code claims spot-verified in the tree (2a6c3d1)
- U-1.5: exactly two `ConfigComponentWriter` call sites in `ServerBuilder.php` (lines ~872 add,
  ~1096 remove); both `lockAndCheckComponent()` SELECTs now include `ID` (lines 5344/5355);
  `DUAL_WRITE_ENABLED` read follows the FLAGS.md pattern, default off.
- U-L.1: `ResourceCatalog::provides()` fail-closed `CatalogException` throws for cpu/nic/unknown
  confirmed in code; no callers yet (grep clean).
- U-L.1's bonus finding for U-1.6 is CORRECT: `UnifiedSlotTracker` identifies risers by
  `component_subtype === 'Riser Card'` at 3 sites (lines 1341, 1479, 1516). `RISER_SUBTYPE_KEYS`
  in `equivalence_report.php` should be narrowed accordingly — assign to whoever next touches that
  file (U-L.3 or a small follow-up).
- U-1.6's flag #3 is REAL and still unfixed: `extractComponentsFromJson()`'s ram block (lines
  ~85-95) never extracts `serial_number` (its cpu block does). **Consequence: once DUAL_WRITE is
  on with real data, equivalence_report will RED every dual-written RAM entry that has a serial.**
  This legacy 3-line fix should be scheduled as (or inside) a unit BEFORE dual-write is enabled or
  the P2 equivalence gate will be noise. Recommend folding into U-L.2's session box if the human
  approves, else its own micro-unit.

## Process note (for the human)
The implementing session covered three units (P1 gate + U-1.6 + U-L.1... etc.) and self-verified
its own units, contra SESSION_PROTOCOL rule 1 and the implemented/verified split. This verify
session found no defects from it — but the split exists to catch what self-review can't; future
sessions should return to one-unit-per-session and leave `verified` to a separate session.

## Outstanding items (unchanged)
1. **ResourceCatalog gaps** (U-L.1 handoff): cpu PCIe-lane capacity + nic SFP-port count throw;
   motherboard cpu_socket/dimm_slot and chassis drive-bay counts missing. Needs a human with real
   `ims-data/` to paste the confirmed field names (or a session allowed to read
   PcieLaneBudgetValidator.php / NICPortTracker.php spec-field usage). Must be resolved before the
   ledger flag is ever turned on, or CPU/NIC adds fail-closed under it.
2. RAM serial_number legacy fix (above) before dual-write on.
3. Seeders 2026_07_06_001..003 still not applied to production DB (manual step).
4. Real-data equivalence/baseline runs still pending a machine with the production dump.

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-5 | PASS — writer hooks propagate exceptions; catalog throws on unknowns |
| INV-8 | PASS — equivalence report GREEN on scratch; self-test detects induced diff |
| INV-11 (this session) | PASS — verification-only: phase-status.json + this handoff |

## Next Prompt To Use
"Continue the IMS migration. Read migration/00-overview/README.md,
migration/ARCHITECTURAL_INVARIANTS.md, then execute unit U-L.2 using
migration/06-resource-ledger/execution-packs/U-L.2.md. Read
migration/handoffs/P1-PL-VERIFY-20260707.md first (outstanding items — especially the
ResourceCatalog cpu/nic gaps and the RAM serial_number legacy fix). ONE unit only this session;
leave 'verified' flips to a separate verify session. Follow
migration/00-overview/SESSION_PROTOCOL.md exactly."

## Files To Load Into Context (next session)
- migration/00-overview/README.md
- migration/ARCHITECTURAL_INVARIANTS.md
- migration/handoffs/P1-PL-VERIFY-20260707.md
- migration/handoffs/U-L.1-20260707.md
- migration/06-resource-ledger/execution-packs/U-L.2.md

## Expected Context Size
~25k tokens
