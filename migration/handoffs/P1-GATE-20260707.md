# Handoff — P1-GATE — 2026-07-07

## Current State
P1 (`02-schema`) gate flipped `closed` -> `open`. U-1.5 and U-1.6 flipped `implemented` ->
`verified` (U-1.1..U-1.4 were already `verified` from the earlier P1-VERIFY-20260706 session). All
six P1 units are now `verified`; P1's `gate_reports` (`schema`, `regression`) both pass. The PR
carrying U-1.5/U-1.6 (branch `claude/migration-directory-impl-yk39zk`, PR #8) was merged into
`main` by the human owner before this session started; this session rebuilt the branch from
`origin/main` (a lossless fast-forward — `git merge-base --is-ancestor` confirmed true first) and
performed the gate verification on top of that merged state.

## Verification performed
Rebuilt the scratch MariaDB (same instance/datadir reused across every unit this migration).
- `schema_report.php` -> GREEN, exit 0; `--self-test` -> "PASS (induced defect correctly flagged)".
- `tests/regression/finalized_immutability_test.php` -> ALL PASS (10/10).
- `tests/regression/nested_transaction_test.php` -> ALL PASS (13/13).
- `DUAL_WRITE_ENABLED=on php tests/regression/dual_write_test.php` -> ALL PASS (16/16).
- `tests/unit/config_component_repository_test.php` -> ALL PASS (20/20).
- `tests/regression/fail_closed_test.php` -> 6/7 checks PASS; the one failure
  (`setup: tmp ram spec exists before hiding`) is the same pre-existing, environment-only gap
  documented in every prior session's handoff this migration: this sandbox has no
  `/home/user/ims-data/` at all, so the test's own fixture-copy step has nothing to copy from. This
  is not a regression from U-1.5/U-1.6 — it predates them (the test was written in U-0.1), and P0's
  gate was already opened with this identical gap present, establishing precedent that it does not
  block a gate decision. The other 6 checks in that file (the actual fail-closed assertions) all
  pass.
- `php scripts/verify/run_all.php --quick` -> schema/inventory/orphan/equivalence all GREEN.
- `php scripts/verify/run_all.php --gate P1` -> schema GREEN; regression SKIPPED (no dedicated
  report script exists for it per the registry — verified manually above instead, per the same
  approach the P1-VERIFY-20260706 session used).
- `php tests/characterize_compatibility.php` -> exit 0, 0 configs/0 replays (no production dump in
  this sandbox); golden baseline restored via `git checkout --` immediately after, confirmed via
  `git status --short` showing a clean tree.

## Completed Work
- `migration/phase-status.json`: P1 gate `closed` -> `open`; `U-1.5`/`U-1.6` `implemented` ->
  `verified`.

## Remaining Work
- Next unit per the execution order is the first `not_started` unit following P1: `PL`
  (`06-resource-ledger`), starting with `U-L.1`. PL's gate lists `["schema", "ledger",
  "regression"]` — `ledger_report.php` doesn't exist yet (lands in U-L.3 per the registry).

## Known Risks
- Same environment gap as every session this migration: no `ims-data/`, no production dump. Every
  gate opened in this sandbox (P0, now P1) has been opened on that basis — a real fleet-scale
  verification (real `characterize_compatibility.php` baseline, real `fail_closed_test.php` fixture)
  is still owed on a machine with the real data, whenever that becomes available.
- Two items flagged in `migration/handoffs/U-1.6-20260706.md` remain open and unconfirmed (best-guess
  pciecard/riser subtype JSON key; the pre-existing `extractComponentsFromJson()` ram
  serial_number-drop quirk). Neither blocked this gate (P1's gate_reports are `schema`+`regression`,
  not `equivalence`), but both should be resolved before `equivalence_report.php` becomes load-bearing
  for a gate that does require it (P2, P6, P8, P9 all list `equivalence`).

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-9 (seeder/rollback pairing) | PASS — no new seeders this session (gate-verification only). |
| INV-11 (unit box) | PASS — 1 file touched (`phase-status.json`), this handoff. |

## Acceptance Test Results
(see "Verification performed" above — all commands and results listed there)

## Next Prompt To Use
"Continue the IMS migration. Read migration/00-overview/README.md, migration/ARCHITECTURAL_INVARIANTS.md,
then execute unit U-L.1 using migration/06-resource-ledger/execution-packs/U-L.1.md (P1's gate is
now open, so PL may begin). Follow migration/00-overview/SESSION_PROTOCOL.md exactly."

## Files To Load Into Context (next session)
- migration/00-overview/README.md
- migration/ARCHITECTURAL_INVARIANTS.md
- migration/06-resource-ledger/execution-packs/U-L.1.md
- Whatever file/line ranges U-L.1's pack lists

## Expected Context Size
~25k tokens
