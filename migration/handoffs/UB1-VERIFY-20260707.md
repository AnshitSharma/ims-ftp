# Handoff — INDEPENDENT VERIFY: U-B.1 — 2026-07-07

## Current State
U-B.1 verified (`implemented` → `verified`). P2 gate stays closed (U-B.2..U-B.4 not started).
Verified against commit `894e50c` on `claude/migration-directory-impl-yk39zk`.

## What was re-run (scratch MariaDB, this container)
- Seeder `2026_07_08_001` round-trip: apply → rollback → re-apply, all clean (INV-9).
- All four backfill modes exercised end-to-end with a fresh fixture config
  (`VERIFY-UB1-1`, one legacy RAM entry), independently of the implementing session's fixtures:
  - **dry-run** (`--config`): plan JSON written; write-free PROVEN via before/after
    `CHECKSUM TABLE` over server_configurations + config_components + config_resources (identical).
  - **--execute --run-id VUB1RUN**: `done=0 quarantined=1 errors=0`; state row `quarantined`,
    one quarantine row with reason `extractor-not-implemented` (stub behavior as specified).
  - **--resume --run-id VUB1RUN**: quarantined config correctly left alone
    (`done=0 quarantined=0 errors=0 verified=0`), no duplicate work.
  - **--rollback-run VUB1RUN**: cleared 0 component rows (stub never inserts — correct),
    all quarantine + state rows removed (0 left).
- SIGPIPE fix verified in code: `reverifyDoneConfig()` drains both pipes via
  `stream_get_contents()` before `fclose()`/`proc_close()` (backfill.php:164-173), with the
  race documented in a comment.
- Full battery: all 5 regression + 2 unit test files PASS; `run_all.php --quick` exit 0
  (schema/inventory/orphan/equivalence GREEN). `php -l` clean. Fixture cleaned up; golden
  baseline untouched.

## Notes for U-B.2 (carried from U-B.1's handoff, endorsed by this verify)
1. The real Extractor must apply the `hbacard_uuid`/`hbacard_config` dedup (stub deliberately
   omits it) — otherwise `uq_inventory_once` will reject the duplicate insert (loud, not silent).
2. `--rollback-run`'s component/ledger deletion path is still untested against real rows —
   U-B.2 must add that test once real inserts exist.
3. The INV-6 question (hard-deleting a component row whose insert bumped `revision`) must be
   answered by whichever unit makes `--execute` insert for real.
4. Backfill inserts go through `ConfigComponentRepository`/ledger writes — the ResourceCatalog
   gaps (cpu/nic provides, RV-1..RV-4) will bite U-B.2+ directly. The human's ims-data field
   names remain the single biggest unblock available.

## Outstanding items (cumulative, unchanged otherwise)
See migration/handoffs/PL-GATE-20260707.md list; plus the three U-B.2 notes above.

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-9 | PASS — seeder pair round-trips |
| INV-5 | PASS — per-config errors quarantine/error-mark loudly; nothing skips silently |
| INV-11 (this session) | PASS — verification-only: phase-status.json + this handoff |

## Next Prompt To Use
"Continue the IMS migration. Read migration/00-overview/README.md,
migration/ARCHITECTURAL_INVARIANTS.md, then execute unit U-B.2 using
migration/07-component-migration/execution-packs/U-B.2.md. Read
migration/handoffs/UB1-VERIFY-20260707.md first (four notes for U-B.2). ONE unit only;
leave 'verified' to a separate session. Follow migration/00-overview/SESSION_PROTOCOL.md exactly."

## Files To Load Into Context (next session)
- migration/00-overview/README.md
- migration/ARCHITECTURAL_INVARIANTS.md
- migration/handoffs/UB1-VERIFY-20260707.md
- migration/handoffs/U-B.1-20260707.md
- migration/07-component-migration/execution-packs/U-B.2.md

## Expected Context Size
~25k tokens
