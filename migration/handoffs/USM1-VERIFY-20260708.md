# Handoff — INDEPENDENT VERIFY: U-SM.1 + seeder-history restore — 2026-07-08

## Current State
U-SM.1 verified (`implemented` → `verified`). P3 gate stays closed (U-SM.2..4 pending — and
NOTE: P3 was started while P2's gate is still closed awaiting human-gated U-B.4; recorded as an
accepted order deviation since U-SM.1 is schema-only and flag-off. U-SM.4 — which replaces the
U-0.2 TEMP-GUARD — must NOT proceed until P2's gate opens).
Also this session: the 18 seeder files deleted from the working tree by commit `75a04bc`
("Migration") were restored from git history, byte-identical, without touching any current file.

## What was re-run (scratch MariaDB, this container)
- Seeder `2026_07_10_001` applied against fixture rows covering every legacy state:
  - inventory: Status 0/1/2 → `failed`/`available`/`installed` — exact per-row match.
  - config: configuration_status 0/1/2/3 → `draft`/`validated`/`building`/`finalized` — matches
    the PACK's stated defensive mapping verbatim (1/2 unreachable per audit; NOT a defect).
  - Idempotent: re-apply leaves existing `status_v2` untouched (`WHERE status_v2 IS NULL`).
- INV-9 round-trip: rollback drops all 11 columns; re-apply clean.
- `schema_report.php` GREEN with the extended expected_schema.json (all 11 new columns + enum
  byte-exact); `run_all.php --quick` exit 0; `--gate P3` exit 0 (schema + inventory GREEN,
  regression covered by hand).
- Full battery: 5 regression + 2 unit + 2 backfill test files, all PASS.

## Seeder-history restore (flagged by the U-SM.1 session; fixed here)
Commit `75a04bc` deleted `database/seeders/` (18 files, 2026_06_11 … 2026_06_24) while adding
migration scaffolding. Restored all 18 verbatim from `75a04bc^` via `git show` (only files that
did NOT exist in the tree were written; `cmp` proves byte-identity). Rationale: CLAUDE.md and the
migration's own rules treat seeders as permanent schema history; the deletion also broke
`ls database/seeders/ | tail -5`-based numbering guidance and U-1.1-style historical reads.

## Reminders
- Seeder `2026_07_10_001` is NOT yet applied to production (same as 2026_07_06_001..003 and
  2026_07_08_001 — five seeders now pending manual production application, in date order).
- U-B.4 runbook exists and waits on the human (see UB2-UB3-VERIFY + the runbook itself).
- The characterization "ZERO diffs" leg of U-SM.1's pack still deserves one real-data run on the
  dev machine (no production dump in this container — standing limitation).

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-9 | PASS — apply/rollback/re-apply round-trip clean |
| INV-11 (this session) | PASS — verification + history restore only; no code changed |

## Next Prompt To Use
Preferred order: U-B.4 (human) before further P3 units. If the human explicitly chooses to keep
working ahead: "Execute unit U-SM.2 using migration/03-state-machines/execution-packs/U-SM.2.md.
ONE unit only; leave 'verified' to a separate session. U-SM.4 must wait for P2's gate."

## Files To Load Into Context (next session)
- migration/00-overview/README.md
- migration/ARCHITECTURAL_INVARIANTS.md
- migration/handoffs/USM1-VERIFY-20260708.md
- migration/03-state-machines/execution-packs/U-SM.2.md

## Expected Context Size
~20k tokens
