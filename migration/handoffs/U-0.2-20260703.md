# Handoff — U-0.2 — 2026-07-03

## Current State
`ServerBuilder::addComponent()` and `::removeComponent()` both reject mutation of a finalized
(`configuration_status=3`) config immediately after acquiring the `SELECT ... FOR UPDATE` row
lock, before any other work happens. This is deliberately temporary scaffolding
(`TEMP-GUARD(U-0.2)`) — U-SM.4 will replace it with the real state machine, U-D.4 deletes it.

## Completed Work
- `core/models/server/ServerBuilder.php`: added the guard block (per pack, verbatim) right after
  the existing "config not found" check in both `addComponent` (uses `$lockedConfigRow`,
  `$ownTransaction`) and `removeComponent` (uses `$config`, no `$ownTransaction` var — the
  guard's `isset($ownTransaction)` ternary handles that without touching removeComponent's
  existing unconditional-rollback pattern). Both marked `// TEMP-GUARD(U-0.2): removed by U-SM.4`.
- `tests/regression/finalized_immutability_test.php` (new): scratch config seeded with
  `configuration_status=3` and one RAM component in its JSON; asserts both `addComponent` and
  `removeComponent` return `error_type=config_finalized`, leave `ram_configuration` and
  inventory `Status` byte-identical, and leave no open PDO transaction. Also a static check that
  both `TEMP-GUARD(U-0.2)` markers are present (count === 2).

## Remaining Work
(empty — unit complete)

## Known Risks
- None new this unit — straightforward, matched the pack exactly, no scope deviations.
- Same MySQL/php.exe local-tooling note as U-0.1 applies (XAMPP, started manually this session).

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-4 (finalized immutable) | PASS — `finalized_immutability_test.php` all green |
| INV-10 (pin before change) | PASS — U-0.1's `fail_closed_test.php` re-run clean before/after; baseline diffed 0 changes |
| INV-11 (unit box) | PASS — 1 file modified (ServerBuilder.php, ~24 LOC) + 1 file created (~95 LOC test), 0 seeders, 1 concept (INV-4) |

## Acceptance Test Results
- `php -l core/models/server/ServerBuilder.php` → No syntax errors detected
- `php tests/regression/finalized_immutability_test.php` → ALL PASS (exit 0)
- `php tests/regression/fail_closed_test.php` → still ALL PASS (exit 0)
- `php tests/characterize_compatibility.php` → 15 configs / 100 replays, diffed against pre-edit baseline: **0 differences** (harness replays validation methods directly, not addComponent/removeComponent, so this guard isn't in that code path — expected)

## Next Prompt To Use
"Continue the IMS migration. Read migration/00-overview/README.md, migration/ARCHITECTURAL_INVARIANTS.md,
then execute unit U-0.3 using migration/01-foundation/execution-packs/U-0.3.md. Follow
migration/00-overview/SESSION_PROTOCOL.md exactly."

## Files To Load Into Context (next session)
- migration/00-overview/README.md (~90 lines)
- migration/ARCHITECTURAL_INVARIANTS.md (~120 lines)
- migration/01-foundation/execution-packs/U-0.3.md
- files/ranges that U-0.3's pack lists (not yet read this session)

## Expected Context Size
~15k tokens (sum of above; keep under 60k)
