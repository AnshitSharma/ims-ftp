# Handoff — U-1.2 — 2026-07-06

## Current State
`config_resources` exists (paired seeder), the capacity/consumption ledger substrate: one row per
provider grant or consumer draw against a slotted or scalar resource (CPU sockets, DIMM slots,
PCIe slots/lanes, M.2/U.2 bays, drive bays, SFP ports, PSU watts, riser slots). Real FKs into
`config_components(id)` — `ON DELETE CASCADE` on `provider_id`, `ON DELETE RESTRICT` on
`consumer_id`. Nothing writes to it yet (same as U-1.1: no code path references this table).
`schema_report.php` (from U-1.1) now also validates `config_resources` via the extended
`expected_schema.json` — no code change to `schema_report.php` itself was needed.

## Note on U-1.1's flagged decision
This unit does NOT depend on it. `config_resources.provider_id`/`consumer_id` reference
`config_components.id` (the auto-increment primary key), which is unaffected either way U-1.1's
`uq_inventory_once` question resolves — that key doesn't touch `id`. Proceeded without waiting for
confirmation on that specific point; the flag in `migration/handoffs/U-1.1-20260706.md` still
stands for whoever builds the code that WRITES to `config_components` (dual-write, backfill).

## Completed Work
- `database/seeders/2026_07_06_002_create-config-resources.sql` (new): creates `config_resources`
  per the pack's DDL verbatim — `config_uuid` collation copied from U-1.1's rule
  (`utf8mb4_unicode_ci`, matching `server_configurations.config_uuid`), `uq_discrete` on
  `(config_uuid, resource, slot_ref)`, real FKs `fk_cr_config`/`fk_cr_provider` (CASCADE)/
  `fk_cr_consumer` (RESTRICT). Header documents the provider-row-vs-consumer-row modeling
  (capacity is always a positive count; providers have `consumer_id IS NULL`, consumers have it
  set) and why scalar resources (`pcie_lane`, `psu_watt`) legitimately have multiple `slot_ref IS
  NULL` rows under `uq_discrete` (NULL-distinctness works in this table's favor, unlike U-1.1's
  `uq_inventory_once` case where it worked against the intended semantics).
- `database/seeders/rollback/2026_07_06_002_create-config-resources_rollback.sql` (new):
  `DROP TABLE IF EXISTS config_resources;` — safe, nothing references the table yet.
- `scripts/verify/expected_schema.json`: added `config_resources`' columns/indexes (17 lines).
  `schema_report.php` itself needed no changes — its checks are already generic over
  `expected_schema.json`'s table list.

## Remaining Work
(empty — unit complete)

## Known Risks
- Same scratch-MariaDB environment as every prior session in this container (no production dump,
  no `ims-data/`). Reused the already-running instance from this session's U-1.1 work.
- Went beyond the pack's literal "Tests Required" list to actually exercise the human checklist
  item ("RESTRICT on consumer FK verified by attempting provider delete in scratch (must fail)")
  with a real fixture rather than leaving it purely as a manual checkbox: built one config with a
  motherboard (provider) and a RAM stick (consumer), one `dimm_slot` provider row and one consumer
  row, then confirmed (a) deleting the consumer component while referenced as `consumer_id` fails
  with MySQL error 1451 (FK RESTRICT, exit 1) and (b) deleting the provider component cascades and
  removes both `config_resources` rows (exit 0, 0 rows remaining). Both matched the intended
  design exactly. Fixture cleaned up afterward.
- `characterize_compatibility.php`: same as every prior session, 0 configs/0 replays (no dump);
  working tree confirmed clean after (`git checkout --` on the regenerated empty baseline).

## Invariant Check Results
| Invariant | Result |
|---|---|
| INV-9 (paired migrations) | PASS — rollback present, both directions tested clean, seeder idempotent. |
| INV-11 (unit box) | PASS — 2 files created (seeder, rollback) + 1 file modified (expected_schema.json, +17 lines), ~88 new LOC, 1 seeder, 1 concept (resource ledger substrate). |
| INV-10 (pin before change) | PASS — no code path changed; working tree unchanged after characterization run. |

## Acceptance Test Results
(all against the hand-built scratch MariaDB)
- `mysql scratch < database/seeders/2026_07_06_002_create-config-resources.sql` → exit 0
- `php scripts/verify/schema_report.php` → GREEN, exit 0 (validates both config_components and
  config_resources)
- Rollback → exit 0, table dropped; re-apply → exit 0; re-apply again (idempotence) → exit 0
- RESTRICT/CASCADE FK behavior verified with a real fixture (see Known Risks) — both exactly as designed
- `php tests/characterize_compatibility.php` → exit 0, 0 configs/0 replays (no dump); working tree
  clean afterward
- `php scripts/verify/run_all.php --gate P1` → exit 0 (`schema: GREEN`, `regression: SKIPPED`)
- `php scripts/verify/run_all.php --gate PL` → exit 0 (`schema: GREEN`, `ledger: SKIPPED`,
  `regression: SKIPPED`)
- `tests/regression/finalized_immutability_test.php` / `nested_transaction_test.php` → still ALL
  PASS (unaffected)

## Next Prompt To Use
"Continue the IMS migration. Read migration/00-overview/README.md, migration/ARCHITECTURAL_INVARIANTS.md,
then execute unit U-1.3 using migration/02-schema/execution-packs/U-1.3.md. Note the still-open
flagged design decision in migration/handoffs/U-1.1-20260706.md (config_components row reuse via
UPDATE vs append via INSERT) — confirm it's still the intended reading before writing any dual-write
code that touches config_components. Follow migration/00-overview/SESSION_PROTOCOL.md exactly."

## Files To Load Into Context (next session)
- migration/00-overview/README.md
- migration/ARCHITECTURAL_INVARIANTS.md
- migration/handoffs/U-1.1-20260706.md (flagged decision — still needs human confirmation)
- migration/02-schema/execution-packs/U-1.3.md

## Expected Context Size
~20k tokens
